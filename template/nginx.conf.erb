server {
    listen  <%= ENV['OPENSHIFT_RINX_IP'] %>:<%= ENV['OPENSHIFT_RINX_PORT'] %>;
    root    <%= ENV['OPENSHIFT_REPO_DIR'] %>public;

    location / {
        index  index.html index.htm;
    }
}